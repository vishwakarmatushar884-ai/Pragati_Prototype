# SSL / TLS Certificates Directory

Place your custom SSL/TLS certificates here for HTTPS deployment:
- `pragati.crt` or `fullchain.pem`
- `pragati.key` or `privkey.pem`

Alternatively, use the automated Let's Encrypt script provided in `scripts/init-ssl.sh`.
